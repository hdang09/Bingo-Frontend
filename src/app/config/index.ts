const routes = {
  login: '/login',
  home: '/',
  rooms: '/rooms',
  waiting: '/waiting',
  create: '/create',
  bingo: '/bingo',
  drawn: '/drawn',
};

const config = {
  routes,
};

export default config;
