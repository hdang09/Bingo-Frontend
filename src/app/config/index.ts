const routes = {
  login: '/login',
  home: '/',
  rooms: '/rooms',
  waiting: '/waiting',
  create: '/create',
  bingo: '/bingo',
  drawn: '/drawn',
};

const langs = ['en', 'vi', 'jp', 'ko'];

const config = {
  routes,
  langs,
};

export default config;
