const routes = {
  login: '/login',
  home: '/',
  rooms: '/rooms',
  waiting: '/waiting',
  create: '/create',
  bingo: '/bingo',
  drawn: '/drawn',
};

const langs = {
  en: 'English',
  vi: 'Vietnamese',
  ja: 'Japanese',
  ko: 'Korean',
};

const config = {
  routes,
  langs,
};

export default config;
