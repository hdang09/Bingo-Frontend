const routes = {
  login: '/login',
  loginGoogle: '/api/v1/auth/login/google',
  home: '/',
  rooms: '/rooms',
  waiting: '/waiting',
  create: '/create',
  bingo: '/bingo',
  drawn: '/drawn',
};

const langs = {
  english: 'en',
  vietnamese: 'vi',
  japanese: 'ja',
  korean: 'ko',
};

const config = {
  routes,
  langs,
};

export default config;
